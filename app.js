document.addEventListener('DOMContentLoaded', ()=> {
    let platform = navigator.platform;
    let location = window.location;
    dupa.innerHTML = 'platform and loaction ' + platform + ' ' + location;
})